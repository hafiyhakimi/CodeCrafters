apiVersion: v1
kind: Service
metadata:
  name: codecrafters-service
spec:
  type: LoadBalancer
  selector:
    app: serving-web-content-codecrafters
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080